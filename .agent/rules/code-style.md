---
trigger: always_on
---

Do not add comments to the code, unless necessary to describe unintuitive actions.
But do not remove any existing comments that were present before.

Use modern python typing (e.g. dict[str] instead of typing.Dict[str])

Use pytest for tests.