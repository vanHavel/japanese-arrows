---
trigger: always_on
---

In the .agent/docs folder, you can find .md files with additional information. Read them if and only if you think the context could be useful for a task.

Always use make or uv to run python project commands such as execute scripts, run tests etc.

After every bigger refactoring, make sure to run all commit hooks and fix any linting issues.